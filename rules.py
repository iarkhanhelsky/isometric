def summ(n):
    return (1 + n) * n // 2


def rule(i, j, max_rows, max_cols):
    return


def generate(n, m, max_rows, max_cols):
    return [[rule(i, j, max_rows, max_cols) for j in range(m)] for i in range(n)]